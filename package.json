{
  "scripts": {
    "dev": "electron app --debug",
    "package:mac": "electron-packager app --overwrite --platform=darwin --arch=x64 --out=out --icon=assets/app-icon/mac/app.icns --osx-sign.identity='Developer ID Application: GitHub' --extend-info=assets/mac/info.plist"
  },
  "devDependencies": {
    "electron": "^1.7.10",
    "electron-packager": "^10.1.1"
  }
}
